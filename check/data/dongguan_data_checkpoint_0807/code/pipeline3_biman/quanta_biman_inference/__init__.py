"""Quanta X1 biman 32D GR00T inference.

Package ``__init__`` stays import-light so ``xr_lerobot`` can run
``live_capture`` / ``live_sdk_daemon`` without installing GR00T.
Import submodules explicitly, e.g. ``from quanta_biman_inference.policy import load_policy``.
"""
