# pipeline7_dongguan_relative_eef_only — Task Manifest (same as pipeline5)
#
# Step0–3: reuse pipeline5 tmp (step1_action_align → step2_15fps → step3_rot6d)
# Step4+: export state 32D / action 20D (eef+gripper only)

defaults:
  source_fps: 31.0
  target_fps: 15.0
  min_duration_sec: 4.0

tasks:
  - task_id: "3"
    text: "Use only your right hand to grasp the door handle. Keep your left arm still."
    include: true
    min_duration_sec: 4.0
    max_duration_sec: null

  - task_id: "4"
    text: "Use only your right hand to rotate the door handle to unlock. Keep your left arm still."
    include: true
    min_duration_sec: 5.5
    max_duration_sec: 14.0

  - task_id: "5"
    text: "Use only your right hand to pull the cabinet door open. Keep your left arm still."
    include: true
    min_duration_sec: 5.5
    max_duration_sec: 12.0

  - task_id: "10"
    text: "Use only your right hand to rotate the black rotary switch. Keep your left arm still."
    include: false

  - task_id: "15"
    text: "Use only your right hand to rotate the black rotary switch. Keep your left arm still."
    include: false

  - task_id: "18"
    text: "Use only your right hand to rotate the door handle to 90 degrees and hold the door in place. Keep your left arm still."
    include: false

  - task_id: "19"
    text: "Use only your right hand to rotate the door handle to close and lock the cabinet door. Keep your left arm still."
    include: false
